# OciDeck — Open-Source Licence Compliance

OciDeck is released under the **EUPL-1.2** (see [`../LICENSE.md`](../LICENSE.md)).
This document records the policy that the project only includes open-source
software, how that is verified, and the result of the latest check.

## Policy

Every dependency and every bundled asset must be available under an OSI-approved
open-source licence. No proprietary or source-unavailable components are shipped.

Accepted licence families: **MIT, BSD (2-/3-Clause), Apache-2.0, MPL-2.0, ISC,
Zlib, BSL-1.0, Unlicense, SIL OFL-1.1, CC0** (and EUPL-1.2 for OciDeck itself).
Anything else — in particular GPL/AGPL/LGPL or a missing/unknown licence — is
flagged for review before it can be added.

## How to verify (repeatable)

A script scans the resolved package graph (direct **and** transitive) and
classifies each licence:

```sh
make licenses          # or: dart run tool/check_licenses.dart
```

It exits non-zero if any package has an unrecognised or non-open-source licence,
so it also runs as part of `make check-full` and can be wired into CI.

> The script reads each package's `LICENSE` file from `.dart_tool/package_config.json`,
> so run `flutter pub get` first. Re-run it whenever dependencies change.

Bundled (non-package) runtime assets — the JavaScript inlined into the HTML
export and the bundled fonts — are tracked by hand in
[`../THIRD_PARTY_NOTICES.md`](../THIRD_PARTY_NOTICES.md).

The authoritative, version-pinned, machine-readable inventory of **every**
component (packages, vendored bundles, forks, fonts, SDKs) — each with its
classified licence — is the Software Bill of Materials, generated by `make sbom`
and documented in [`SBOM.md`](SBOM.md). It reuses the exact same licence
classifier as `make licenses` (`tool/license_detect.dart`), so the two can never
disagree.

## Latest result

All **187** resolved Dart/Flutter packages (including OciDeck itself) use
recognised open-source licences — *as of 2026-07-22*:

| Count | Licence |
| ---: | --- |
| 123 | BSD-3-Clause |
| 47 | MIT |
| 13 | Apache-2.0 |
| 1 | MPL-2.0 (`dbus`, Linux only) |
| 1 | BSD |
| 1 | BSL-1.0 |
| 1 | EUPL-1.2 (OciDeck itself) |

> A count in prose goes stale the moment a dependency lands, and nothing fails
> when it does. The **generated** equivalent is the licence table at the top of
> [`../sbom/ocideck.sbom.md`](../sbom/ocideck.sbom.md), rebuilt by `make sbom`
> and held current by `make sbom-verify` — read that one if the two disagree.

Counting the whole SBOM (199 components — packages plus the vendored JS bundles,
bundled fonts, plugin forks and SDKs) adds 5 × OFL-1.1 (the bundled fonts) and
1 × `Apache-2.0 OR MPL-2.0` (DOMPurify), and lifts BSD-3-Clause to 127, MIT to 49
and Apache-2.0 to 14.

Bundled assets: marked (MIT), highlight.js (BSD-3-Clause), Mermaid (MIT, bundling
DOMPurify under Apache-2.0/MPL-2.0), MathJax (Apache-2.0), five fonts under SIL
OFL-1.1 (EB Garamond, Roboto, Inter, Lora — licence texts in `assets/fonts/`) and
the YuNet face-detection model (MIT). The OciDeck-owned brand images in
`assets/images/` and the theme in `assets/themes/` are the project's own work.

**Conclusion: no non-open-source software is included.**

## What the user redistributes

Two of OciDeck's outputs make the *user* the distributing party, so the notices
have to travel inside them rather than only living in this repository:

- **The app itself.** The four OFL licence texts (`assets/fonts/OFL.txt`,
  `Inter-OFL.txt`, `Lora-OFL.txt`, `Roboto-OFL.txt`) are declared assets and are
  reachable in the running app under **Settings → Documentatie → Licenties van
  derden**, which also lists the YuNet model and the vendored forks. OFL-1.1 §2
  requires the copyright notice and licence to accompany the font; shipping the
  `.ttf` without them would not.
- **The HTML export.** Every exported deck inlines five third-party bundles and
  embeds the EB Garamond font as base64. The export therefore opens with a
  third-party notice block, and each inlined `<script>` carries its own licence
  banner. MathJax (Apache-2.0) and Mermaid (MIT) ship minified *without* the
  banner their upstream build strips; OciDeck re-adds it, because a user who
  mails an export cannot discharge a duty we removed the evidence of.

`marp_html_service_licenses_test.dart` fails if a bundle listed in
`assets/web_export/MANIFEST.json` reaches the export without a notice.

## Bundled reference data (information-security module)

The optional *Informatieveiligheid* module (off by default) bundles reference
data. **The data ships in every build, module on or off** (PENTEST_MIAUW §6), so
these terms apply to every copy of OciDeck, not only to users who enable the
module.

The table below is the complete set — it mirrors, one for one, the catalogues the
app lists under **Settings → Uitbreidingen**, so what a user is told is available
locally and what is recorded here cannot drift apart:

| Dataset | Where | Upstream | Licence / terms |
|---|---|---|---|
| CWE — curated floor (40 weaknesses, with our own remediation notes) | `lib/services/cwe_catalog.dart` | MITRE CWE | MITRE Terms of Use — **attribution to MITRE** |
| CWE — full list (969 weaknesses: id, name, description) | `assets/cwe/cwe_full.json`, generated by `tool/build_cwe_catalog.dart` from MITRE's Comprehensive CWE CSV | MITRE CWE 4.20 | MITRE Terms of Use — **attribution to MITRE**. Names and descriptions are MITRE's text, slimmed but not rewritten |
| OWASP WSTG checklist index (97 tests: id, canonical title, category) | `lib/services/wstg_catalog.dart` | OWASP Web Security Testing Guide v4.2 | **CC-BY-SA-4.0 — attribution + share-alike** (see below) |
| OWASP MASWE weakness list (117 weaknesses: id, title, MASVS category, platform, CWE mapping) | `lib/services/maswe_catalog.dart` (+ `_written`/`_draft` parts), generated by `tool/build_maswe_catalog.dart` | OWASP Mobile Application Security Weakness Enumeration, snapshot **2026-06-12** (the project publishes no releases or tags) | **CC-BY-SA-4.0 — attribution + share-alike** (same terms as WSTG, below) |
| OWASP MASTG test index (186 tests: id, canonical title, MASVS category, MASWE link) | `lib/services/mastg_catalog.dart` (+ `_android`/`_ios` parts), generated by `tool/build_mastg_catalog.dart` | OWASP Mobile Application Security Testing Guide **v2.0.0** | **CC-BY-SA-4.0 — attribution + share-alike** (same terms as WSTG, below) |
| Health lexicon for the privacy check (62,490 rare-disease names in nl/en/de/fr/es/it/pl/pt/cs) | `assets/privacy/health_lexicon.json`, generated by `tool/build_privacy_lexicon.dart` from Orphanet's `product1` files | Orphanet nomenclature, release **2026-06-23** (orphadata.com) | **CC-BY-4.0 — attribution required.** The attribution string ships inside the asset itself and the generator refuses to build if the source ever declares anything other than CC-BY-4.0 |
| Belief lexicon for the privacy check (1,536 terms — religions, ideologies, union and ethnicity — in 27 languages) | `assets/privacy/belief_lexicon.json`, generated by `tool/build_eurovoc_lexicon.dart` from a EuroVoc SPARQL query | EuroVoc, subtrees under *godsdienst*, *politieke ideologie*, *vakbond* and *etnische groep* | **EU reuse policy (Decision 2011/833/EU) — attribution required.** The attribution string ships inside the asset; 15 concepts that describe the *topic* rather than a person (church, theology, holy books) are excluded by concept URI, so the exclusion holds in all 27 languages |
| MIAUW EIS catalog (full 88-EIS schema) | `lib/services/miauw_eis_catalog.dart` | MIAUW workbook, as of **2026-07-16** (github.com/brennodewinter/Informatiebeveiligingsonderzoek) | EUPL-1.2 (same as OciDeck) — clean to bundle |
| CVSS v4.0 engine + MacroVector table (270 rows) | `lib/services/cvss/` | FIRST.Org CVSS v4.0 specification | Native-Dart reimplementation of the public spec; **attribution to FIRST.Org**. The qualitative band labels (None/Low/Medium/High/Critical) are FIRST's published rating scale and are reproduced verbatim |
| Finding templates (3 starters × 32 languages) | `lib/services/finding_templates/` | OciDeck's own | EUPL-1.2 — our content. The `cwe:` line inside each template is a MITRE citation (above) |

> **Versions in this table are mirrored from
> [`lib/services/reference_standards.dart`](../lib/services/reference_standards.dart)**,
> which is the single source of truth for what this build bundles.
> `reference_standards_test.dart` fails if the two disagree, and
> `tool/check_reference_data.dart` (wired into `make deps-check`) reports when a
> bundled version has fallen behind upstream. Before this existed, versions lived
> only in prose here — and prose cannot be checked, which is how a catalogue goes
> stale unnoticed.

### OWASP WSTG, MASTG and MASWE — attribution and share-alike

All three bundled OWASP datasets carry the same licence and the same reasoning; the
WSTG wording below applies verbatim to the MASTG index, with one difference
worth stating: from MASTG only the **v2.0.0** components are taken. The 92
tests OWASP marked `deprecated` in that release and the 14 unwritten
placeholders are deliberately left out — bundling a withdrawn or unwritten test
would put a check in a client's report that nobody can perform.

From MASWE the opposite call was made, for a reason worth stating: its
unwritten entries **are** bundled (marked), because a weakness that OWASP has
identified — with an id, a title and a CWE mapping — is legitimately citeable
even while its guidance page is empty. Withdrawn weaknesses are excluded.
MASWE also publishes no releases or tags, so what is recorded is the date of
the snapshot rather than a version number.

**What is bundled:** the WSTG **checklist index** — for each of the 97 tests its
stable id (`WSTG-INFO-01`), its canonical English title ("Conduct Search Engine
Discovery Reconnaissance for Information Leakage") and its category. It fills a
`checklist` slide with the standard's test list in one click. The guide's actual
substance — how to test, what to look for, the remediation guidance — is **not**
bundled; every entry points back to owasp.org for that.

**Attribution.** The Web Security Testing Guide is © the OWASP Foundation and its
contributors, published under
[CC-BY-SA-4.0](https://creativecommons.org/licenses/by-sa/4.0/). OciDeck
reproduces the v4.2 checklist index unmodified in substance, reformatted as Dart
data. Source: <https://owasp.org/www-project-web-security-testing-guide/>.

**Share-alike.** The CC-BY-SA-4.0 terms travel with that dataset: anyone
redistributing it — as part of OciDeck or lifted out of it — does so under
CC-BY-SA-4.0 with this attribution. Share-alike attaches to **the WSTG material**,
not to OciDeck: the app bundles it as a *Collection* (the licence's own term for
including a work alongside other, separate works), so the EUPL-1.2 code around it
is unaffected. Reformatting into Dart structures is the one point where a reader
could argue *Adapted Material* rather than mere reproduction; either way the
obligation is the same one already discharged here — attribute, name the licence,
and keep the dataset under it.

> **This paragraph used to say the opposite.** It claimed test standards were
> referenced "by name only" and that no WSTG checklist content was bundled, so
> share-alike was not triggered — while `wstg_catalog.dart` had carried the
> 97 verbatim titles all along. The paragraph even named the condition for its own
> undoing ("should verbatim checklist content be bundled later…"); nobody came
> back to it when the module landed. Recorded here so the next reader knows this
> section was checked against the code, not assumed.

**Referenced by name only** (no content bundled, so nothing to license): PTES,
OWASP ISTG and OWASP FSTM, used for the scope-to-standard mapping (Infra→PTES,
IoT→ISTG, Firmware→FSTM). Nominative use of a standard's name carries no
licensing obligation. Should any of their content be bundled later — the same way
WSTG's was — it belongs in the table above **in the same commit**.

> **And it happened again, with MASTG.** This list named OWASP MASTG as
> referenced-by-name-only while the table above — in this same file — already
> accounted for **186 verbatim MASTG test titles** under CC-BY-SA-4.0, split
> across `mastg_catalog_android.dart` (98) and `mastg_catalog_ios.dart` (88).
> The Mobile→MASTG mapping is still a nominative use, but the catalogue is not,
> and the table is the entry that governs. MASTG is removed from this list;
> *corrected 2026-07-21*. The failure mode is identical to the WSTG one above:
> the sentence was written before the content landed and nobody came back to it.
> If a third standard's content is ever bundled, the same two places have to
> move together.

## A note on Apache-2.0 and the EUPL

A few components are Apache-2.0 (e.g. MathJax in the HTML export, and some Dart
packages). Using Apache-2.0 libraries as unmodified dependencies in an EUPL-1.2
work is fine. Note, however, that Apache-2.0 is **not** on the EUPL's list of
"compatible licences" (which governs the *outbound* relicensing of derivative
works under Article 5 EUPL). This only matters if you create a combined
derivative work that must be relicensed; it does not affect bundling these
libraries as-is. If you need formal certainty for a specific distribution
scenario, have it confirmed by someone with licence expertise.

### The case this section did not cover: a *modified* Apache-2.0 component

The paragraph above only settles the *unmodified dependency*. OciDeck has one
component that is neither: `third_party/desktop_multi_window` is a **fork with
local source changes**, and it is Apache-2.0 (© 2021 Mixin) — not MIT, which is
what this project recorded until 2026-07-22. Modification triggers obligations
the unmodified case does not:

| Apache-2.0 clause | What it asks | How OciDeck discharges it |
| --- | --- | --- |
| §4(a) | Give recipients a copy of the licence | `third_party/desktop_multi_window/LICENSE` ships in the repository and in every source distribution |
| §4(b) | **Modified files must carry prominent notices stating that you changed them** | Each of the six changed files opens with a `NOTICE:`-block naming the change, the upstream commit and the licence |
| §4(c) | Retain attribution notices from the source form | The upstream copyright line is kept verbatim in `LICENSE`; no upstream header was removed |
| §4(d) | Carry a `NOTICE` file's contents if the original has one | Upstream ships no `NOTICE` file, so nothing to carry |

`third_party/desktop_multi_window/MODIFICATIONS.md` is the human summary: the
upstream commit the fork descends from, why it exists, and a per-file table of
what changed. The SBOM records the same commit plus a SHA-256 tree hash of the
directory, so "which source is this, exactly" has a machine-readable answer too.

**Outbound, this changes nothing.** The fork stays under Apache-2.0 and is
distributed as a separate component alongside OciDeck's EUPL-1.2 code, exactly
as the unmodified Apache-2.0 packages are; our changes to it are contributed
under that same Apache-2.0, not relicensed to EUPL. The Article 5 question above
would only arise if the two were merged into one work needing a single outbound
licence, which they are not.

> **Why this is written down.** The trap is genuinely sharp: the GitHub API
> reports **MIT** for `MixinNetwork/flutter-plugins`, because that is the
> monorepo's *root* LICENSE. The package directory carries its own Apache-2.0
> file, and that one governs. Any tooling that classifies a fork by repository
> rather than by the vendored directory will make the same mistake. Ours no
> longer guesses: `tool/sbom_build.dart` classifies both forks from the LICENSE
> file on disk with the same classifier `make licenses` uses.
